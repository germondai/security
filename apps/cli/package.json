{
  "name": "@germondai/security-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "gsec": "./bin/gsec.mjs"
  },
  "scripts": {
    "lint": "echo 'lint: skipped (eslint not configured yet)'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'test: no CLI tests in v0.1'",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist .tsbuildinfo .turbo",
    "gsec": "node bin/gsec.mjs"
  },
  "dependencies": {
    "@clack/prompts": "^1.6.0",
    "@germondai/security": "workspace:*",
    "citty": "^0.2.2",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@germondai/tsconfig": "workspace:*",
    "@types/bun": "^1.3.14",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3"
  }
}
